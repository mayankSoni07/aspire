/**
 * Main page component for the Aspire Cards application
 * This is the home route (/) that renders the entire banking card management interface
 * Simply renders the AppShell component which contains all the application logic
 */
import AppShell from "@/components/AppShell";

export default function Home() {
  return <AppShell />;
}
